// CineFind configuration template
// Copy this file to config.js and add your keys

window.CONFIG = {
  // Watchmode API — two keys provisioned (use KEY_1 for primary, KEY_2 as fallback)
  WATCHMODE_API_KEY_1: "",
  WATCHMODE_API_KEY_2: "",

  // Default region for streaming sources (e.g., US, PT, BR)
  DEFAULT_REGION: "US"
};
